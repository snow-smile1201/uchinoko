<% if @activities.present? %>
  <%= link_to "通知のクリア", inform_activities_path, method: :delete, class:"btn btn-light"%>
  <% @activities.each do |activity| %>
    <%= render "#{activity.action_type}", activity: activity %>
  <% end %>
<% else %>
  <p>通知はありません</p>
<% end %>