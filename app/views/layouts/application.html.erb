<!DOCTYPE html>
<html>
  <head>
    <title>Uchinoko</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name"viewport" content="width=device-width, initial-scale=1, ,maximum-scale=1">
    <script src="https://kit.fontawesome.com/0ccf06e068.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="d-flex flex-column vh-100">
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <!--ここにロゴイメージ挿入予定-->
          <a class="navbar-brand p-3" href="/"><strong>Uch</strong><i class="fa-solid fa-child-reaching"></i><strong>noco</strong></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expand="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse flex-column" id="navbarNavDropdown">
            <%if user_signed_in? %>
              <%= render partial: "layouts/users_nav_list"%>
            <%elsif admin_signed_in? %>
              <%= render partial: "layouts/admin_nav_list"%>
            <% else %>
            <ul class="navbar-nav ml-auto">
              <li class="nav-item">
                <%= link_to "registration", new_user_registration_path, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "login", new_user_session_path, class: 'nav-link text-dark' %>
              </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main class="mb-auto">
      <%= flash[:notice] %>
      <%= yield %>
    </main>
    <footer class="bg-dark text-white pt-3 pb-2">
      <div class="container">
        <div class="row no-gutters">
          <div class="col-md-4">
            <p>うちの子育てを共有するSNS</p>
          </div>
          <div class="col-md-8">
            <h6 class="text-white mt-2 mb-2">Link</h6>
            <ul class="p-0 list-unstyled">
              <li>
                <a href="" title="About" class="text-white-50">コンタクト</a>
              </li>
              <li>
                <a href="" title="License informations" class="text-white-50">プライバシーポリシー</a>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
